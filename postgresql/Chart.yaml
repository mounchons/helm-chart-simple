apiVersion: "v2"
name: Simple PostgreSQL Kubernetes
version: "1.0.0"
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    repository: https://charts.bitnami.com/bitnami
    version: 15.2.9


# ต้องอ้างอิงใน path ที่สร้างไฟล์ chart
#  cd postgresql/
# helm dependency update
# helm upgrade -i postgre-simple-dev . -n dev --create-namespace

# render
# helm template .